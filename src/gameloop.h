// gameloop.h
#ifndef STDIO_H
#define STDIO_H
#include <stdio.h>
#endif
#ifndef STDBOOL_H
#define STDBOOL_H
#include <stdbool.h>
#endif

#ifndef GAMELOOP_H
#define GAMELOOP_H

void gameloop() {
		while(true) {
		puts("entered the game loop");
		// we can construct it as if then else the whole way down…
		// or we can build a structure to store our messages and have references that chart out the whole thing.
		//

		// either way, we need the script for the game at this point.
		break;
	}
}

#endif